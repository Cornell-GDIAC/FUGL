# FUGL

Figma plugin for exporting CUGL scene nodes as JSON.

## Instructions

Install dependencies:

```sh
pnpm install
```

Build:

```sh
pnpm build
```

Build (Watch):

```sh
pnpm dev
```

Export as archive:

```sh
pnpm zip # creates release.zip
```
